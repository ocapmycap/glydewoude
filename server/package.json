{
  "name": "@glidewood/server",
  "private": true,
  "version": "0.1.0",
  "description": "Glidewood backend: accounts, persistence, and server-side validation of the economy.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "dev": "node --watch --env-file-if-exists=.env src/index.js",
    "migrate": "node --env-file-if-exists=.env src/db/cli-migrate.js",
    "seed": "node --env-file-if-exists=.env src/db/cli-seed.js"
  },
  "dependencies": {
    "@glidewood/shared": "*",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.14"
  }
}
